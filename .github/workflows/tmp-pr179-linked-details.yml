name: Temporary PR179 linked details fix

on:
  push:
    branches:
      - fix/home-event-unification
    paths:
      - .github/workflows/tmp-pr179-linked-details.yml
      - scripts/tmp_fix_pr179_linked_details.py

permissions:
  contents: write

jobs:
  patch-and-test:
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 20
    steps:
      - name: Patch and validate
        run: |
          set -euo pipefail
          WORK=/tmp/marketai-pr179-linked-details
          PY=/home/marko/marketai-repo/.venv/bin/python
          rm -rf "$WORK"
          git clone --branch fix/home-event-unification --single-branch https://github.com/Calvonen/market-memory-bot.git "$WORK"
          cd "$WORK"
          python3 scripts/tmp_fix_pr179_linked_details.py
          "$PY" -m unittest tests.test_mobile_source -v
          cd mobile
          npm ci
          npx tsc --noEmit
          npm run lint
          cd ..
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "mobile/src/app/(tabs)/index.tsx" mobile/src/components/TrackedEventsSection.tsx tests/test_mobile_source.py
          git commit -m "fix: preserve linked tracked event details"
          git push origin HEAD:fix/home-event-unification
